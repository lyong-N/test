apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: my-dep
spec:
  #replicas: 3
  selector:
    matchLabels:
      app: my-dep
  template:
    metadata:
      labels:
        app: my-dep
    spec:
      containers:
      - image: ly-pods
        name: nginx
 
